declare module '@env' {
  /** Inlined from `.env` at Metro bundle time (`react-native-dotenv`). */
  export const WALLET_THEME: string | undefined
  /** Same as native launcher label when listed in Babel `allowlist`. */
  export const APP_DISPLAY_NAME: string | undefined
}
